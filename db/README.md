# Database Directory

This directory is a placeholder for future database work.

- `migrations/` — SQL migration files (numbered, e.g. `001_create_debts.sql`)
- `schemas/` — Schema definitions and reference

No database is wired yet. See `docs/db-evolution-plan.md` and `docs/neon-migrations-plan.md` for the plan.
