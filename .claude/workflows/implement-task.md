# Implement Task

1. Read relevant docs and skills files
2. Understand the task — clarify with user if ambiguous
3. Create or modify files following repo conventions
4. Ensure loading, empty, error, and edge case states
5. Run lint: `npm run lint`
6. Verify build: `npm run build`
7. Report what was done (see `task-reporting` skill)
