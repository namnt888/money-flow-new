# Parallel Session Rules

When running multiple sessions in parallel:
1. Each session must read this file before starting
2. Do NOT modify files that another session is working on — coordinate via the user
3. Each session produces a final task report
4. Prefer creating new files over modifying files another session may touch
5. If in doubt, ask the user which session handles what
